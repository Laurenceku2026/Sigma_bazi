# Optional CJK fonts for PDF

Bundled for Streamlit Cloud (embedded into PDF, avoids black squares):

- `NotoSansSC-Regular.ttf` — Noto Sans SC (OFL), via fontsource subset

Do not commit proprietary Windows fonts (SimHei / Microsoft YaHei).
